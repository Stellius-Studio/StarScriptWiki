## Description
True if the server's tick loop is currently frozen (via `setfrozen` or the vanilla
`/tick freeze` command).


## Attributes
None.


## Examples
```yaml
Skills:
- message{m="The server is frozen."} ?isserverfrozen @trigger
```


## Aliases
None.
